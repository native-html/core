coverage:
  precision: 2
  round: down
  range: '80...100'
  status:
    project:
      default:
        target: auto
      css-processor:
        target: auto
        flags:
          - css-processor
      transient-render-tree:
        target: auto
        flags:
          - transient-render-tree

flags:
  css-processor:
    paths:
      - packages/css-processor/coverage/cover.xml
  transient-render-tree:
    paths:
      - packages/transient-render-tree/coverage/cover.xml
